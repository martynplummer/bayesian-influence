# UNOS common file

Files common to the Bernoulli and binomial implementations of the UNOS model

## Inputs

* `unos-data.csv` Data tabulated by Gelfand (2003) in CSV format
* `unos-init.R` Defines initfun() which generates initial values for all models
